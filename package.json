{
  "name": "extension-gpt",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "build": "turbo run build --include-dependencies",
    "build:ui": "turbo run build --filter=ui",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.4.2"
  },
  "packageManager": "pnpm@8.0.0",
    "workspaces": {
    "packages": ["apps/frontend", "packages/*"]
  }
}
